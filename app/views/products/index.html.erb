<h1>Products</h1>
<%= link_to("Wanna host your own haunted doll? Beep bebeep?", "/products/new") %>

  <% @products.each do |product| %>
  <div class="product">
    <h3><%= link_to("#{product.name}", "/products/#{product.id}") %></h3>
    <p><%= product.price_format %></p>
    <p><%= product.description %></p>
  </div>
<br>
<br>
<br>
<% end %>