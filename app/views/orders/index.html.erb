<h1>Upcoming curses:</h1>

<div>
  <% @orders.each do |order| %>
    <%= link_to("#{order.id}: #{order.total}", "/orders/#{order.id}", class: "list-group-item list-group-item-action") %>
  <% end %>
</div>